#spring配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/b1?serverTimezone=GMT%2B8&useSSL=true
    username: root
    password: ly
#服务器配置
server:
  port: 8888
#mybatis配置
mybatis:
  mapper-locations: classpath:mapper/*.xml
#日志配置
logging:
  config: classpath:logback-spring.xml
